<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Reading a user-picked video via SAF doesn't need broad storage permission,
         but we request the media-video permission for gallery pickers on API 33+
         and WRITE for saving the collage on API < 29 (scoped storage handles 29+). -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".CollageApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="iykyk Collage"
        android:supportsRtl="true"
        android:theme="@style/Theme.IykykCollage"
        android:largeHeap="true">

        <!-- ML Kit: bundle the face detection model in the APK so first run is fast
             and fully offline (no Play Services download step). -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IykykCollage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
